# ============================================
# FIREBASE CONFIGURATION
# ============================================
# Get these from Firebase Console > Project Settings > General > Your apps > Web app
# URL: https://console.firebase.google.com/

NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ============================================
# CLOUDFLARE R2 CONFIGURATION
# ============================================
# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens
# URL: https://dash.cloudflare.com/ > R2 Object Storage

# Account ID: Found in Cloudflare Dashboard URL or R2 Overview page
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id

# R2 Access Key ID: Create from R2 > Manage R2 API Tokens > Create API Token
CLOUDFLARE_R2_ACCESS_KEY_ID=your_r2_access_key_id

# R2 Secret Access Key: Shown only once when creating the API token
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_r2_secret_access_key

# Bucket Name: The name of your R2 bucket (create one if not exists)
CLOUDFLARE_R2_BUCKET_NAME=wiseco-images

# Public URL: Enable public access in R2 bucket settings > Public Access
# Format: https://pub-{hash}.r2.dev OR your custom domain
NEXT_PUBLIC_CLOUDFLARE_R2_PUBLIC_URL=https://your-bucket-public-url.r2.dev

# ============================================
# JWT SECRET FOR AUTHENTICATION
# ============================================
# Generate a secure random string (min 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your_super_secure_jwt_secret_key_min_32_chars

# ============================================
# ADMIN CREDENTIALS (Initial Setup)
# ============================================
# These are used to create the initial admin user
# Change the password after first login!
ADMIN_EMAIL=admin@wiseco.id
ADMIN_PASSWORD=WisecoAdmin2025!
